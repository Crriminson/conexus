# Conexus rebuild — standing rules

## Stack
React + TypeScript + Vite + Tailwind + shadcn/ui + wouter (routing) + Framer Motion + react-hook-form + Zod + Zustand (global state)
Backend/DB: Supabase (Postgres + Auth + Storage)

## Process
- Work proceeds as a sequence of numbered "Phase" prompts, one per message.
- Each Phase touches only the file(s)/tab it's scoped to — never modify a previous Phase's files unless a Phase explicitly says to.
- After finishing each Phase, stop and wait for review before continuing to the next.

## Data model
- The shared data model lives in `src/types/project.ts`, defined once in Phase 0.
- Extend its implementation across phases — never casually change its shape.

## AI content rules
- AI-generated content must always carry a citation back to its source.
- Numeric/eligibility figures are never generated by an LLM — only by the deterministic rules engine.
